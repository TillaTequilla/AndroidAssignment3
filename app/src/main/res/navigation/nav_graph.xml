<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authFragment3">

    <fragment
        android:id="@+id/authFragment3"
        android:name="com.example.androidAssignment3.screens.AuthFragment"
        android:label="AuthFragment">
        <action
            android:id="@+id/action_authFragment3_to_profileFragment"
            app:destination="@id/profileFragment" />
    </fragment>
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.androidAssignment3.screens.ProfileFragment"
        android:label="ProfileFragment">
        <action
            android:id="@+id/action_profileFragment_to_authFragment3"
            app:destination="@id/authFragment3" />
        <argument
            android:name="user_name"
            app:argType="string"
            android:defaultValue="David Deshko" />

    </fragment>
</navigation>